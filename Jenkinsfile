pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // 빌드 단계 작업 정의
            }
        }
        stage('Test') {
            steps {
                // 테스트 단계 작업 정의
            }
        }
        // 추가적인 단계들 정의 가능
    }
    // post 섹션, 확장된 설정 가능
    post {
        // 성공 또는 실패 시의 처리 정의 가능
    }
}
